import { useState } from "react"


export default function ApplicationsTable({ applications, refreshApplications }: { applications: any[], refreshApplications: () => void }) {
    

    return (
        <table></table>
    )
}
